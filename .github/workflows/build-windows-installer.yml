name: Build Windows Installer

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string

jobs:
  build-windows-installer:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Publish CalcpadCE WPF
        run: dotnet publish Calcpad.Wpf/Calcpad.wpf.csproj -c Release -r win-x64 --self-contained false -p:Version=${{ inputs.version }}

      - name: Publish CalcpadCE CLI
        run: dotnet publish Calcpad.Cli/Calcpad.Cli.csproj -c Release -r win-x64 --self-contained false -p:Version=${{ inputs.version }}

      - name: Stage CLI into WPF publish directory
        shell: pwsh
        run: |
          $wpfPublish = "Calcpad.Wpf/bin/Release/net10.0-windows/win-x64/publish"
          $cliPublish = "Calcpad.Cli/bin/Release/net10.0/win-x64/publish"
          New-Item -ItemType Directory -Path "$wpfPublish/cli" -Force | Out-Null
          Copy-Item -Path "$cliPublish/*" -Destination "$wpfPublish/cli" -Recurse

      - name: Install NSIS
        run: choco install nsis -y --no-progress

      - name: Install NScurl NSIS plugin
        uses: negrutiu/nsis-install-plugin@v1
        with:
          github-owner: negrutiu
          github-repo: nsis-nscurl
          github-asset-regex: NScurl\.zip

      - name: Build Installer
        working-directory: Calcpad.Wpf/Installer
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path Output -Force | Out-Null
          & "${env:ProgramFiles(x86)}\NSIS\makensis.exe" /DAPP_VERSION="${{ inputs.version }}" CalcpadCE.nsi

      - name: Upload installer artifact
        uses: actions/upload-artifact@v7
        with:
          path: Calcpad.Wpf/Installer/Output/CalcpadCE-setup-${{ inputs.version }}.exe
          archive: false
